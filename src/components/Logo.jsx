const Logo = () => {
  return (
    <div className="bg-transparent flex justify-center max-w-max max-h-max  items-center">
      <h1 className="font-sans text-4xl -tracking-[0.03em] font-extrabold text-transparent bg-clip-text bg-logo-gradient">
        StarBucks
      </h1>
    </div>
  );
};

export default Logo;
